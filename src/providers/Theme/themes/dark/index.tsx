import { Theme } from '../theme'

import devices from '../../devices'

import color from './color'
import colors from './colors'

const theme: Theme = { name: 'dark', devices, color, colors }

export default theme
